/**
 * Shared Types - Export tất cả
 * Folder này chứa types/interfaces dùng chung giữa Backend và Frontend
 * 
 * LƯU Ý:
 * - CHỈ chứa type definitions (interfaces, types, enums)
 * - KHÔNG chứa business logic
 * - KHÔNG chứa implementations
 * - KHÔNG import thư viện bên ngoài (trừ built-in types)
 */

export * from './movie.types';
export * from './booking.types';
export * from './showtime.types';
export * from './api.types';
